# Ngày 28 — HTTP Client và JSON

Học tạo HTTP request bằng `HttpClient`. Ví dụ chỉ tạo request, không gửi ra
Internet nên chạy được offline.

## Bài tập

Gửi GET tới một API thử nghiệm, thêm timeout và xử lý status code.
